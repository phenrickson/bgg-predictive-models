# Google Cloud Platform Configuration

# Data warehouse project (read data source, write predictions)
DATA_WAREHOUSE_PROJECT_ID=bgg-data-warehouse

# ML artifacts project (models, experiments, GCS buckets)
ML_PROJECT_ID=bgg-predictive-models

# Service account credentials
GOOGLE_APPLICATION_CREDENTIALS=credentials/ml-service-account-key.json

# BGG API token for collection loading
BGG_API_TOKEN=

# Environment for ML artifacts (dev/prod/test)
ENVIRONMENT=dev
